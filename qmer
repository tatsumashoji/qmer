#!/bin/bash

fn=`basename $1`
cat $fn | grep -v ^@ | cut -f10 | uniq -c | awk '{print$1,$2}' > $fn.tp
python make_fasta.py $fn.tp
python count_qmer.py $fn.tp2 14
rm $fn.tp $fn.tp2
mkdir -p $2
mv $fn.qmer_* $2

